# ConnectorHtmlHelperFactory.php

**Chemin :** `include/connectors/utils/ConnectorHtmlHelperFactory.php`
**Type :** PHP
**Derniere mise a jour doc :** 2026-06-02

---

## Role fonctionnel

Fabrique pour instancier `ConnectorHtmlHelper`. Permet de substituer l'implementation du helper HTML (ex: pour les tests ou customisations). Utilise dans `ConnectorUtils::getConnectorButtonScript()`.

## Role technique

INCONNU — fichier non lu dans ce contexte. Probablement une classe statique avec methode `build()` retournant un `ConnectorHtmlHelper`.

---

## Exports / Symboles principaux

| Symbole | Type | Role |
|---|---|---|
| `ConnectorHtmlHelperFactory` | classe | Fabrique de helper HTML |
| `build(): ConnectorHtmlHelper` | methode statique probable | Cree une instance |

- **Consommateurs identifies :** `ConnectorUtils::getConnectorButtonScript()`

---

## Points d'attention

- Fiche incomplete — fichier non lu dans ce contexte.
