# CalendarConnectionTestResult.php

**Chemin :** `include/CalendarSync/domain/valueObjects/CalendarConnectionTestResult.php`
**Type :** PHP
**Derniere mise a jour doc :** 2026-06-02

---

## Role fonctionnel

Value object encapsulant le resultat d'un test de connexion a un fournisseur de calendrier externe. Transporte le statut de succes/echec et un message explicatif.

## Role technique

INCONNU — fichier non lu dans ce contexte. Probablement un objet immuable avec `$success: bool` et `$message: string`.

---

## Exports / Symboles principaux

| Symbole | Type | Role |
|---|---|---|
| `CalendarConnectionTestResult` | classe value object | Resultat de test de connexion |

- **Consommateurs identifies :** `CalendarSync::testProviderConnectionWithValidation()`, `AbstractCalendarProvider::testCalendarConnection()`

---

## Points d'attention

- Fiche incomplete — fichier non lu dans ce contexte.
