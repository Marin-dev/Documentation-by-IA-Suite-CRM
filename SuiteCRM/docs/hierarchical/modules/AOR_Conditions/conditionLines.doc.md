# conditionLines.php (AOR_Conditions)

**Chemin :** `modules/AOR_Conditions/conditionLines.php`
**Type :** PHP - Helper (rendu de formulaire)
**Derniere mise a jour doc :** 2026-05-31

## Role fonctionnel
Fournit la fonction `display_condition_lines()` appelee dans les vues EditView du rapport AOR pour afficher dynamiquement les lignes de conditions de filtrage.

## Relations cles
- **Appele via vardefs :** champ `condition_lines` de type `function` dans `AOR_Reports/vardefs.php` (ligne 119)
- **Consomme :** donnees POST et beans `AOR_Condition` existants

## Points d'attention
- Contenu exact INCONNU — fichier non lu directement.
