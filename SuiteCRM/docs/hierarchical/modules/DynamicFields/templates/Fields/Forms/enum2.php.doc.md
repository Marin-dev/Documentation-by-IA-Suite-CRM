# Fichier : enum2.php (Forms)

**Chemin :** `modules/DynamicFields/templates/Fields/Forms/enum2.php`
**Type :** PHP — Helper formulaire Studio (variante enum)
**Derniere mise a jour doc :** 2026-05-31

---

## Role fonctionnel

Variante du formulaire de configuration Studio pour les champs de type liste deroulante. Role exact vis-a-vis de `enum` standard : INCONNU (difference de configuration non determinee a la lecture du fichier).

## Role technique

Script PHP de rendu formulaire. Structure identique aux autres fichiers Forms.

---

## Relations cles

- **Appele par :** INCONNU — non reference explicitement dans `FieldViewer::getLayout()`
