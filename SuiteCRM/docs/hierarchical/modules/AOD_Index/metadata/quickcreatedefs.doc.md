# quickcreatedefs.php

**Chemin :** `modules/AOD_Index/metadata/quickcreatedefs.php`
**Configure :** Mise en page du formulaire de creation rapide du module AOD_Index
**Derniere mise a jour doc :** 2026-05-31

---

## Ce que ce fichier configure
Definit `$viewdefs['AOD_Index']['QuickCreate']` — le formulaire de creation rapide (popup). Panneau unique avec `name` et `assigned_user_name`. Layout 2 colonnes.

## Parametres cles
| Panneau | Champs |
|---|---|
| `default` | `name`, `assigned_user_name` |

## Impacte par / impacte
- Charge par le framework SugarCRM pour les formulaires de creation rapide (subpanels, popups)

## Points d'attention
- RAS — formulaire minimal standard.
