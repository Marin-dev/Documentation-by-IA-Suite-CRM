# vardefs.php (EmailTemplates)

**Chemin :** `modules/EmailTemplates/vardefs.php`
**Type :** PHP — configuration/vardefs
**Dernière mise à jour doc :** 2026-05-31

---

## Rôle
Definit le schema de la table `email_templates` via `$dictionary`. Champs cles : `name`, `subject`, `body`, `body_html`, `published`, `text_only`, `type`.

**Type :** config
