# Fichier fieldLines.php — AOR_Fields

**Chemin :** `modules/AOR_Fields/fieldLines.php`
**Type :** PHP
**Dernière mise à jour doc :** 2026-05-31

## Rôle fonctionnel
Bibliothèque de rendu HTML pour le widget de saisie des lignes de champs dans le formulaire EditView de AOR_Reports. Génère le tableau interactif des champs sélectionnés avec leurs options (affichage, label, fonction, tri, groupement, lien, total, format).

## Type
helper / vue

## Notes
Invoqué via le function field `field_lines` dans `AOR_Reports/vardefs.php`. La fonction d'entrée est probablement `display_field_lines()`.
