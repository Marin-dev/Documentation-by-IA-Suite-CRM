# fieldLines.php (AOR_Fields)

**Chemin :** `modules/AOR_Fields/fieldLines.php`
**Type :** PHP - Helper (rendu de formulaire)
**Derniere mise a jour doc :** 2026-05-31

## Role fonctionnel
Fournit la fonction `display_field_lines()` appelee dans les vues EditView du rapport AOR pour afficher dynamiquement les lignes de champs selectionnes.

## Relations cles
- **Appele via vardefs :** champ `field_lines` de type `function` dans `AOR_Reports/vardefs.php` (ligne 95)
- **Consomme :** donnees POST et beans `AOR_Field` existants

## Points d'attention
- Contenu exact INCONNU — fichier non lu directement.
