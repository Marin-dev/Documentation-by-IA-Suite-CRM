# Fichier : AM_ProjectTemplates_sugar.php

**Chemin :** `modules/AM_ProjectTemplates/AM_ProjectTemplates_sugar.php`
**Type :** PHP - Modele genere (Model generated)
**Derniere mise a jour doc :** 2026-05-31

---

## Role fonctionnel

Classe de base generee par Module Builder pour `AM_ProjectTemplates`. Definit les proprietes et la configuration de base du bean (table, object_name, champs, importable). Ne doit pas etre modifiee manuellement — les personnalisations vont dans `AM_ProjectTemplates`.

## Role technique

Herite de `Basic`. Declare les proprietes publiques correspondant aux colonnes de la table `am_projecttemplates`. `$disable_row_level_security = true` pour compatibilite CE/PRO. Classe marquee "GENERATED BY MODULE BUILDER — PLEASE DO NOT CHANGE".

---

## Dependances principales

| Import / Classe | Role |
| --- | --- |
| `Basic` | Classe parente SuiteCRM pour les modules custom |

---

## Exports / Symboles principaux

| Symbole | Type | Role |
| --- | --- | --- |
| `AM_ProjectTemplates_sugar` | Classe | Base generee du modele template de projet |

**Table DB :** `am_projecttemplates`

---

## Relations cles

- **Heritee par :** `AM_ProjectTemplates` (couche de personnalisation)
- **Position dans le flux :** Classe intermediaire dans la chaine `AM_ProjectTemplates -> AM_ProjectTemplates_sugar -> Basic -> SugarBean`

---

## Points d'attention

- Ne jamais modifier ce fichier — il peut etre regenere par Module Builder et les changements seraient perdus.
- `$disable_row_level_security = true` desactive la securite par lignes — tous les utilisateurs avec acces au module voient tous les enregistrements.
